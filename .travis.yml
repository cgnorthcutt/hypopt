language: python
matrix:
    include:
        - python: 2.6
          dist: trusty
          sudo: false
        - python: 2.7
          dist: trusty
          sudo: false
        - python: 3.3
          dist: trusty
          sudo: false
        - python: 3.4
          dist: trusty
          sudo: false
        - python: 3.5
          dist: trusty
          sudo: false
        - python: 3.6
          dist: trusty
          sudo: false
        - python: 3.7
          dist: xenial
          sudo: true
        - python: pypy
          dist: trusty
          sudo: false
# command to install dependencies
install:
  - pip install tox-travis
  - pip install pytest
  - pip install -r requirements.txt
  - pip install -e .
# command to run tests
script:
  - tox # or py.test for Python versions 3.5 and below